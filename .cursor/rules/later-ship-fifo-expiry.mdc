---
description: 出貨商品效期預設 FIFO（最近效期），下拉可改；接庫存後再寫 PRD／實作
alwaysApply: true
---

# 之後要做：出貨商品效期先進先出

適用：出貨單 → 新增出貨單 → **出貨商品** 的「效期_批號」欄。

正式環境（有庫存效期資料後）寫 PRD／實作都要照這套：

- **預設**：先進先出（FIFO）。帶入該商品「離目前最近」的效期（最快到期的那一筆）。
- **可改**：下拉選單仍列出其他可選效期／批號，操作人可手動改。
- 不要鎖死只能用預設那筆。

DEMO 尚未接正式庫存。出貨商品帶入（含虛擬倉可覆蓋自動帶入、確認出貨）時，效期下拉用假批號，**預設選離今天最近的那筆**，仍可改。不要接真庫存效期。
