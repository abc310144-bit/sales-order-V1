---
description: 已完成須數量出滿；主管可對部分送達確認完成
alwaysApply: true
---

# 已完成、部分送達與主管確認完成

自動 **已完成**（不必主管）：有效出貨單（狀態不是訂單取消）全部已配達，且各 SKU 有效出貨數量加總等於訂單下訂數量。列表「完成說明」空白。

**部分送達**（仍可新增出貨單）：已有已配達，但數量還沒出滿，或還有單未配達。全部配達但數量沒出滿 → 不要自動已完成，維持部分送達。

主管確認完成（只給主管、檢視「部分送達」時，即使還有出貨單在配送也可按）：
- 訂單 → 已完成，不能再開出貨單
- 完成說明預設「以實收為主」（可改）；列表顯示這段
- 之後 `syncPoStatusFromShips` 不可把這張單回寫掉
- 進行中出貨單狀態不因結案而改成已配達
- 已取消訂單不做此確認
