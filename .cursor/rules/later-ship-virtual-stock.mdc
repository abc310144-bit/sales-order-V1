---
description: 出貨商品依虛擬倉庫存覆蓋剩餘可出量；不足才警示與確認出貨
alwaysApply: true
---

# 出貨商品 vs 虛擬倉庫存

新增出貨單 → 出貨商品：品項、數量對齊下定商品「訂單商品總攬」。

- **品項**：總攬裡數量 > 0 的 SKU 全部帶入，不因虛擬倉不足或為 0 而從表上拿掉。
- **數量**：剩餘可出＝max(0, 下訂 − 已出量)。第一張出貨單（尚未出過）數量＝總攬下訂；待出貨／部分送達再開單＝扣掉已出量後的剩餘。剩餘 ≤ 0 的 SKU 不再帶入。
- **虛擬倉**：只做警示。庫存 < 剩餘時紅字警示＋彈窗；彈窗「下訂數量」顯示剩餘可出。不要把出貨數量改成庫存量，也不要把品項移出出貨商品表。

已出量＝該 SKU 在所有有效出貨單（不是訂單取消）上的數量加總。

DEMO 用假庫存（不接真虛擬倉）。效期預設選假批號裡離今天最近的，下拉可改。
