---
description: 線上出貨訂單建立頁與線上-轉單分流
alwaysApply: true
---

# 線上出貨訂單

選單：品運 → 通路出貨 → **線上出貨訂單**。

## 列表（DEMO）

- 僅 UI：欄位對齊正式畫面；「編輯」「取消訂單」置灰不可用
- 右上「新增出貨單」→ 建立頁（可選採購單）

## 建立頁（所有新增路徑相同）

與一般「建立出貨訂單」共用頁面（`is-online`），但線上出貨：

- **不顯示／不驗證**：貿易條件、預計到貨時間、通路進貨倉點（含取貨方式）
- **費用填寫整區刪除**（不帶入、不儲存費用）
- **仍顯示**：預計拋單日、預計到貨日期、預計出貨日
- **線上專用設定**：平台訂單編號（唯讀，帶入採購單單號）、運送方式*（下拉：新竹物流／黑猫／全盛貨運(邱哥)／宅配通）
- **收件人**（設定下方獨立區塊，皆唯讀）：收件人、收件人電話、收件人地址、備註

可選採購單：僅 **線上-轉單** 且 `未開立出貨單`／`部分出貨`。  
選單後帶入採購單單號→平台訂單編號、收件人等；運送方式必填、自行選擇。

建完停在線上出貨訂單列表；出貨列標 `online: true`（一般出貨列表不顯示）。存檔時上述隱藏欄位與費用皆空白／空陣列。

## 客戶訂單列表批次

- 篩選結果列：核取方塊（僅 **線上-轉單** 且可出貨：未開立出貨單／部分出貨 可勾；其餘 disabled）
- 有勾選時，列表左上顯示「批次建立出貨單」
- 點擊 → 僅選運送方式 → 每張勾選單各建一張線上出貨單（1:1），建完進線上出貨訂單列表
- 同樣不帶貿易條件／到貨時間／倉點／費用
