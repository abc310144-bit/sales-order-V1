---
description: 線上出貨訂單建立頁與線上-轉單分流
alwaysApply: true
---

# 線上出貨訂單

選單：品運 → 通路出貨 → **線上出貨訂單**。

## 列表（DEMO）

- 僅 UI：欄位對齊正式畫面；「編輯」「取消訂單」置灰不可用
- 右上「新增出貨單」→ 建立頁（可選採購單）

## 建立頁

與「出貨訂單管理 → 建立出貨訂單」相同（設定／出貨商品／費用），另加：

- **設定**：平台訂單編號（唯讀）、運送方式*（下拉：新竹物流／黑猫／全盛貨運(邱哥)／宅配通）— 與取貨方式並存
- **收件人**（設定下方獨立區塊，皆唯讀）：收件人、收件人電話、收件人地址、備註

可選採購單：僅 **線上-轉單** 且 `未開立出貨單`／`部分出貨`。  
選單後帶入客戶訂單收件人／平台訂單編號等；運送方式必填、自行選擇。

建完停在線上出貨訂單列表；出貨列標 `online: true`（一般出貨列表不顯示）。

## 客戶訂單入口

檢視「線上-轉單」且可出貨時，「新增出貨單」→ 線上建立頁（採購單鎖死、資料帶入）。  
非轉單仍走出貨訂單管理建立頁。
