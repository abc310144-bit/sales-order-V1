---
description: 線上-轉單收件人區塊與轉單匯入格式
alwaysApply: true
---

# 線上-轉單收件人

觸發：通路名稱＝**線上-轉單**（合作通路類型＝台灣線上通路，採購單類型仍屬一般通路）。

## 檢視／編輯

- 訂單基本資料（線上-轉單）：
  1. 頂列：**買方（左）｜日期區（右）**（採購單單號、預計到貨日期／時間、應出貨時間）
  2. **收件人列表**（表格，一人一列）
  3. **賣方**
- 非線上-轉單：日期區 → 買方 → 賣方（無收件人）
- 收件人列表欄位：通路平台*（唯讀，同步買方「通路名稱」）、收件人姓名*、郵遞區號*（三碼）、地址*、電話*、訂單總金額、運送方式*（新竹物流／黑猫／全盛貨運(邱哥)／宅配通）、備註（有填則 5～30 字）
- 建立／匯入：買方選「線上-轉單」後，收件人各列通路平台自動帶入同值並鎖定；匯入檔「通路平台」欄不寫入收件人（以匯入時選的通路名稱為準）
- 儲存草稿／送出申請均須通過收件人必填檢核
- 匯入檔「預計出貨日」寫入應出貨時間；「預計到貨日」寫入預計到貨日期（皆不上收件人區）
- 匯入檔「平台訂單編號」寫入**採購單單號**（不上收件人區）
- 建立線上出貨單時，運送方式可由收件人帶入（仍可改）

## 匯入客戶訂單

- 選「線上-轉單」時使用 `轉單匯入模板.xlsx`（欄位同轉單 2C）
- 收件人與日期寫入訂單；SKU／數量寫入下定商品；倉別寫入 inWarehouse；單品總金額等附加在品項
- 任一列失敗整檔不匯入；上限 1000 筆
- （之後）多筆平台單合併一客戶訂單、收件人多列、商品加總 → 另議

## 建立

- 手動建立並選線上-轉單：收件人列表可空白一列填寫，存草稿／送出必填

## 新增出貨單

- 線上-轉單：跳「線上出貨訂單」建立頁（見 later-online-ship）
- 其他通路：跳「出貨訂單管理」建立頁

## 下定商品匯入範本

- `下定商品匯入模板.xlsx`：SKU、商品名稱、數量（皆必填；數量正整數）
