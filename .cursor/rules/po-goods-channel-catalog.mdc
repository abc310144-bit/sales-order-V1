---
description: 下定商品由通路虛擬倉挑選後進入總攬，不另開商品頁
alwaysApply: true
---

# 採購商品來源

建立採購單 → 確定 → **進入「訂單基本資料」頁籤**（一般通路、經銷、境外皆同）。再由操作人切「下定商品」。確定後不要自動切到下定商品。不另開、不跳轉獨立的商品頁。頁籤文案三種採購單類型相同：訂單基本資料、下定商品、出貨單。

一般通路基本資料比照經銷／境外：顯示買方／賣方。買方帶入該通路基本資料，賣方帶入饌元公司基本資料。三種單都顯示預計到貨日期、預計到貨時間、應出貨時間（應出貨時間只填日期，沒有時間）。

下定商品頁籤（三種類型相同）：

- **選擇商品**：列出該通路／經銷／境外在「通路供應商品列表」有綁定的品項（含可配數 0、不足 1 箱）。欄位：核取方塊、SKU、商品名稱、效期_批號、當前可賣量、數量設定。當前可賣量＝虛擬倉當前配數。效期_批號為展示欄（DEMO 用系統假批號，格式同出貨單 `日期_倉`），不編輯。
- **訂單商品總攬**：只展示已勾選且數量不為 0 的商品。欄位：SKU、商品名稱、箱入數、訂購數量(pcs)、訂購總箱數、單價（不含標）、單價（含標）、合計、備註。除 **單價（含標）** 外總攬不可編輯。含標不從虛擬倉自動帶值，編輯時空白輸入框；僅可填正數（如 280.00）。未填時檢視與匯出 PDF 該欄顯示 `-`；有填才在 PDF 顯示數值。合計仍只依 **單價（不含標）** 計算。訂購總箱數＝訂購數量(pcs) ÷ 箱入數，可小數（支援散出）。

畫面：
- **檢視**：下定商品頁籤只顯示訂單商品總攬，不顯示選擇商品。
- **建立／編輯**：完整顯示選擇商品＋訂單商品總攬。不可編輯的單（非草稿）即使點編輯也當檢視。

操作：

- 先勾選才能填數量；數量單位 pcs，須為大於 0 的正整數，**不必**為箱入數整數倍（可散出）；可大於當前可賣量，超量時數量欄紅框，仍可進總攬。
- 選擇商品標題旁有「匯入商品」（僅草稿可編輯時）：彈窗選 XLSX／CSV／XLS，可下載範例檔 `下定商品匯入模板.xlsx`（欄位：SKU、商品名稱、效期_批號、數量；皆必填；數量僅正整數）。上限 1000 筆，超過整檔拒絕。任一筆失敗（未綁定通路商品、欄位缺漏、數量非正整數）→ 整檔不匯入並提示。成功 → 覆蓋既有勾選／數量，篩出檔案 SKU、勾選、填數量、總攬同步，提示「成功匯入」。DEMO 商品名稱／效期以系統為準；正式環境需與檔案相互匹配。
- 匯入後選擇商品只顯示檔案內 SKU；搜尋只在目前顯示範圍內。清除篩選：若處於匯入篩選，還原全表並取消勾選／清空數量；一般搜尋清除只清搜尋條件，保留勾選／數量。
- 選擇商品標題下方有篩選：輸入 SKU 或名稱，`,` 或 `，` 分隔，OR 模糊比對（含部分字即可，完整 SKU 即精準）。按搜尋或 Enter 才套用，命中的商品自動勾選核取方塊（可接著填數量）。只篩選擇商品，不篩總攬；被篩掉的勾選與數量仍保留（匯入篩選清除除外）。
- 上表一改，總攬同步更新。沒有確定／重製；按「儲存為草稿」或「送出申請」才寫入。
- 建立新單時總攬從空白開始。複製／已存在的單維持其原商品。
- 複製只給審核通過後的單：待回壓文件、未開立出貨單、待出貨、部分送達、已完成。草稿、待審核、已作廢不顯示複製。複製成新草稿，商品帶入總攬，不帶回壓檔。

出貨訂單管理建立出貨單：選採購單後，若任一 SKU 訂購 pcs > 當前可賣量，先彈警示列全部超量品項（文案：`SKU : … 商品名稱 : …當前可賣量 < 下定量，請確認是否分批出貨`），確定才帶入出貨設定；取消關閉警示。

DEMO 用假虛擬倉與假單價（含不含標），不接真倉。選擇商品不要放「新增商品」「出貨總數量(pcs)」；匯入商品僅放在選擇商品標題旁。

建立出貨單「出貨商品」用舊版採購商品表（不是 PDF 欄）：編號、商品品號、SKU、品名、倉別、效期_批號、允收天數、商品成本(TWD)、商品報價(TWD)、訂單商品報價(TWD)、庫存數、備註、總數量(pcs)、操作。工具列只留出貨總數量(pcs)，不要新增／匯入。可覆蓋的品項自動帶入；不足見 later-ship-virtual-stock。效期 FIFO 見 later-ship-fifo-expiry。建立入口見 later-ship-create-stamp。
